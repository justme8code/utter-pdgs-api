services:
  postgres_db:
    image: postgres:latest
    container_name: postgres_db
    environment:
      POSTGRES_DB: utterfresh_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - utterfresh_network

  utter-pdgs-api:
    container_name: utter-pdgs-api-container
    image: utter-pdgs-api:latest
    depends_on:
      - postgres_db
    ports:
      - "8080:8080"
    networks:
      - utterfresh_network

volumes:
  postgres_data:

networks:
  utterfresh_network:
    driver: bridge